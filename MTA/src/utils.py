# MTA-style span/feature distillation lives in `span_loss.py`.
# The previous attention-weighted span pooling helpers were removed when
# switching to the mta_dskd_v2-style projector + spaCy-based pipeline.
